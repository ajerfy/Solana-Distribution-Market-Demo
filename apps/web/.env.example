# Optional. Leave unset during `npm run dev` to use the Vite proxy (same origin → http://127.0.0.1:8787).
# Set this to hit a remote API instead, e.g. https://api.example.com
# VITE_PARABOLA_API_BASE=http://127.0.0.1:8787
